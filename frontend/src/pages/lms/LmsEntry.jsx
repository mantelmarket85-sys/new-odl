// Thin pass-through to the existing LMS placeholder page.
// (The original file was empty due to an archive-extraction issue; this stub
// simply re-exports the existing LMS component so routing keeps working.)
import LMS from '../LMS';

const LmsEntry = LMS;
export default LmsEntry;
